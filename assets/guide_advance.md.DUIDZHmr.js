import{_ as a,c as t,o as l,ah as r}from"./chunks/framework.D_XWT_8K.js";const h=JSON.parse('{"title":"Advance","description":"","frontmatter":{},"headers":[],"relativePath":"guide/advance.md","filePath":"guide/advance.md"}'),i={name:"guide/advance.md"};function s(o,e,n,c,p,u){return l(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="advance" tabindex="-1">Advance <a class="header-anchor" href="#advance" aria-label="Permalink to “Advance”">​</a></h1><h2 id="rule-priority" tabindex="-1">Rule Priority <a class="header-anchor" href="#rule-priority" aria-label="Permalink to “Rule Priority”">​</a></h2><p>When multiple rules could match the same request, priority matters.</p><ul><li>Rules are evaluated from top to bottom</li><li>The first matching rule will be applied</li><li>You can change priority by dragging rules to reorder them</li></ul><p>This makes it easy to override behavior in a controlled and predictable way.</p><h2 id="using-rule-groups" tabindex="-1">Using Rule Groups <a class="header-anchor" href="#using-rule-groups" aria-label="Permalink to “Using Rule Groups”">​</a></h2><p>For complex projects, rules can be organized into groups.</p><p>Groups help you:</p><ul><li>Separate rules by project or feature</li><li>Enable or disable an entire set of rules at once</li><li>Keep large configurations readable</li></ul><div class="tip custom-block"><p class="custom-block-title custom-block-title-default">TIP</p><p>Grouping does not affect matching behavior — only organization.</p></div><h2 id="applying-changes" tabindex="-1">Applying Changes <a class="header-anchor" href="#applying-changes" aria-label="Permalink to “Applying Changes”">​</a></h2><p>After modifying rules:</p><ul><li>Rule changes are saved automatically</li><li>Click <code>Apply Rules</code> to activate the updated configuration</li><li>Refresh the target web page to see the changes</li><li>No browser restart is required</li></ul><h2 id="revert-rules" tabindex="-1">Revert Rules <a class="header-anchor" href="#revert-rules" aria-label="Permalink to “Revert Rules”">​</a></h2><p>Restores the rule configuration to the <strong><code>last applied snapshot</code></strong>.</p><ul><li>If you modify rules but have not clicked <strong>Apply Rules</strong>, you can use <strong>Revert Rules</strong> to discard the pending changes.</li><li>The rules will be restored to the exact configuration that was last successfully applied.</li><li>This helps you safely undo mistakes or experiments without manually reverting each rule.</li></ul><div class="tip custom-block"><p class="custom-block-title custom-block-title-default">TIP</p><p>Revert Rules only affects unapplied changes. Once rules are applied, they become the new baseline state.</p></div><h2 id="url-pattern-matching" tabindex="-1">URL Pattern Matching <a class="header-anchor" href="#url-pattern-matching" aria-label="Permalink to “URL Pattern Matching”">​</a></h2><p>The <code>remoteUrl</code> field supports <strong>pattern-based matching</strong>, not just exact URLs.</p><p>URL matching follows the same syntax as Chrome’s <code>declarativeNetRequest</code> <code>urlFilter</code> rules, allowing flexible patterns such as:</p><ul><li>Prefix matching</li><li>Substring matching</li><li>Wildcards (<code>*</code>)</li><li>Scheme and domain-based matching</li></ul><p>This makes it possible to override multiple related resources with a single rule.</p><p>For example:</p><ul><li>Match all files under a CDN path</li><li>Proxy a group of API endpoints</li><li>Replace multiple script versions at once</li></ul><p>Because pattern matching can affect multiple requests, it is recommended to:</p><ul><li>Keep URL patterns <strong>as specific as possible</strong></li><li>Avoid overly broad wildcards unless intentionally required</li><li>Carefully review rule priority when multiple patterns overlap</li></ul><p>For detailed matching behavior, refer to the official Chrome documentation:<br><a href="https://developer.chrome.com/docs/extensions/reference/api/declarativeNetRequest#url_filter_syntax" target="_blank" rel="noreferrer">https://developer.chrome.com/docs/extensions/reference/api/declarativeNetRequest#url_filter_syntax</a></p>',27)])])}const g=a(i,[["render",s]]);export{h as __pageData,g as default};
